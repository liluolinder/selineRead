[package]
  cjc-version = "1.1.0"
  name = "seajson"
  version = "1.4.7"
  output-type = "static"
  src-dir = "src"
  compile-option = "-Woff unused"
  description = "SeaJson (释笺) — native JSON infrastructure for Cangjie (pure std, zero external deps)"
  include = ["src", "README.md"]

[dependencies]

# SeaJson is pure Cangjie standard library - no stdx or external dependencies required
